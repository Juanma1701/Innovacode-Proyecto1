<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.InnovacodeProyecto1">

        <!-- Activities existentes -->
        <activity
            android:name=".Perfil.Content.Perfil"
            android:exported="false" />
        <activity
            android:name=".Inventarioform.Content.Inventarioform"
            android:exported="false" />
        <activity
            android:name=".Dashboard.Content.Dashboard"
            android:exported="true" />
        <activity
            android:name=".Inventario.Content.Inventario"
            android:exported="false" />

        <!-- Ventas -->
        <activity
            android:name=".MainVentas"
            android:exported="false" />

        <!-- Compras -->
        <activity
            android:name=".ComprasActivity"
            android:exported="false" />

        <!-- Launcher -->
        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>